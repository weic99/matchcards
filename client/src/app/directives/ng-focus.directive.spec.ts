import { NgFocusDirective } from './ng-focus.directive';

describe('NgFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new NgFocusDirective();
    expect(directive).toBeTruthy();
  });
});
