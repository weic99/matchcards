import { NgScrollDirective } from './ng-scroll.directive';

describe('NgScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new NgScrollDirective();
    expect(directive).toBeTruthy();
  });
});
